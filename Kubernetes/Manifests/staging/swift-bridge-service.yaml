apiVersion: v1
kind: Service
metadata:
  name: swift-bridge-service
spec:
  type: ClusterIP
  selector:
    component: swift-bridge-service
  ports:
    - port: 10000
      targetPort: 80
      name: http-route